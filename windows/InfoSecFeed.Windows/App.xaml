<Application x:Class="InfoSecFeed.Windows.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <SolidColorBrush x:Key="WindowBrush" Color="#090D13" />
        <SolidColorBrush x:Key="PanelBrush" Color="#111923" />
        <SolidColorBrush x:Key="CardBrush" Color="#17212D" />
        <SolidColorBrush x:Key="CardHoverBrush" Color="#1D2B3A" />
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#F2F6FA" />
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="#A7B5C4" />
        <SolidColorBrush x:Key="AccentBrush" Color="#66E3C4" />
        <SolidColorBrush x:Key="DangerBrush" Color="#FF6B7A" />
        <SolidColorBrush x:Key="WarningBrush" Color="#FFBC66" />

        <Style TargetType="Button">
            <Setter Property="Background" Value="#243445" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="16,9" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Surface" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Surface" Property="Background" Value="#31506A" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Surface" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1A2734" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="BorderBrush" Value="#34485C" />
            <Setter Property="CaretBrush" Value="{StaticResource AccentBrush}" />
            <Setter Property="Padding" Value="12,9" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#1A2734" />
            <Setter Property="Foreground" Value="#121820" />
            <Setter Property="Padding" Value="10,7" />
        </Style>
    </Application.Resources>
</Application>
